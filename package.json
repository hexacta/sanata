{
	"name": "sanata",
	"version": "0.0.0",
	"scripts": {
		"start": "run-p start:web start:server",
		"start:web": "npm start --prefix web",
		"start:server": "npm start --prefix server",
		"test": "prettier-check web/src/**/*.js server/src/**/*.js"
	},
	"devDependencies": {
		"npm-run-all": "^4.0.2",
		"prettier": "^0.21.0",
		"prettier-check": "^1.0.0"
	}
}
